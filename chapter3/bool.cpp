#include <iostream>

int main() {
    bool is_ready = true;
    int ans = true;
    int promise = false;

    std::cout << "is_ready: " << is_ready << std::endl;
    std::cout << "ans: " << ans << std::endl;
    std::cout << "promise: " << promise << std::endl;

    return 0;
}